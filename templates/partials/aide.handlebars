<article class="ui card segments aide">
    <h3 class="ui segment">{{ name }}</h3>

    {{#if amount}}
        <div class="ui inverted teal segment aide_amount">
            <span class="aide_amount--value">{{ amount }}</span>
            <span class="aide_amount--unit">&nbsp;€</span>
            <span class="aide_amount--frequency">&nbsp;/&nbsp;mois</span>
        </div>
    {{/if}}

    {{#if conditions}}
        <div class="ui segment">
            Pour en bénéficier, vous devez :
            <ul>
                {{#each conditions as |condition|}}
                    <li>{{{ condition }}}</li>
                {{/each}}
            </ul>
        </div>
    {{/if}}

    <div class="ui segment">
        {{#if url}}
            <a href="{{ url }}">Découvrir {{ prefix }} {{ acronym }}</a>
        {{/if}}
    </div>
</article>
