description: 'Results should have no error',
/** This is an independent feature mostly to mark it as pending until an actual computation is made.
* It should probably be merged into feature #2.
* */
scenario: [
    {
        'ResultsWidget.error' : false,
    }
]
