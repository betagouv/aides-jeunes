export enum ContributionCategory {
  BENEFIT = "benefit",
  INSTITUTION = "institution",
}

export enum ContributionPullRequestStatus {
  PENDING = "pending",
  SUCCEEDED = "succeeded",
  FAILED = "failed",
}
