# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST     /form/:formId                  controllers.Application.generate(formId)
OPTIONS  /*anyroute                     controllers.Application.options(anyroute)
